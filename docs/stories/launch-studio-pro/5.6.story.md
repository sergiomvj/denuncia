# Story 5.6: Copywriter e Joint-Launch Advisor

## Status

Draft

## Executor Assignment

```yaml
executor: "@dev"
quality_gate: "@qa"
quality_gate_tools: ["npm run lint", "npm run typecheck", "copywriter/jl agent tests"]
```

## Story

**As a** desenvolvedor do Launch Studio PRO,
**I want** implementar `copywriter_agent` e `jl_partner_advisor` que geram o pack de copy e o plano de lançamento conjunto,
**so that** o dossiê contenha e-mails prontos, scripts de vídeo PLC, página de vendas e (quando aplicável) lista de parceiros e estrutura de comissão (PRD §6, §2).

## Acceptance Criteria

1. `copywriter_agent` implementado: recebe sequência aprovada (output de 5.4), usa `ModelRouter` com `phase_role='copywriter'`, gera: e-mails (pré-pré, PLC1/2/3, abertura, follow-ups, fechamento) em Markdown/HTML, scripts de vídeo PLC 1/2/3, página de vendas em blocos HTML/copy.
2. `jl_partner_advisor` implementado: recebe `launch_type` (ativo apenas se `launch_type='joint'`) + avatar/nicho (etapa 10), usa `ModelRouter` com `phase_role='planner'`, gera: lista de perfis-alvo de parceiros, script de abordagem, estrutura de comissão (PRD §2, §6).
3. `jl_partner_advisor` é CONDICIONAL: só é executado se `launch_type === 'joint'` (Lançamento Conjunto).
4. Saída do `copywriter_agent` populada em `launch_dossiers.payload.sequence` (e-mails/scripts nas fases) e artefatos separados.
5. Saída do `jl_partner_advisor` populada em `launch_dossiers.payload.joint_launch`.
6. API route `POST /api/launch-studio-pro/projects/[id]/generate/copy` dispara os agentes.
7. Testes de integração com mock do ModelRouter, incluindo cenário sem lançamento conjunto.
8. `npm run lint` e `npm run typecheck` passam.

## 🤖 CodeRabbit Integration

**Primary Type**: API
**Secondary Type(s)**: —
**Complexity**: Large

**Specialized Agent Assignment**:
- Primary: @dev
- Supporting: @qa (completude do pack de copy)

**Quality Gate Tasks**:
- [ ] Pre-Commit (@dev): lint + typecheck + testes
- [ ] Pre-PR (@qa): verificar que copywriter usa Claude Opus (phase_role='copywriter')

**Self-Healing Configuration**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 30 minutos
- Severity Filter: CRITICAL, HIGH

**Focus Areas**:
- Condicional JL: não executar `jl_partner_advisor` se não for lançamento conjunto
- Pack completo: todos os e-mails + scripts das 6 fases PLF
- Modelo correto: copywriter → Claude Opus (via config do banco)

## Tasks / Subtasks

- [ ] Criar `lib/launch-studio-pro/agents/copywriter-agent.ts` (AC: 1)
  - [ ] Input: sequência PLF completa (output de 5.4)
  - [ ] Output: e-mails por fase, scripts PLC 1/2/3, sales_page_outline
  - [ ] Usar `phase_role='copywriter'` (→ Claude Opus via config)
- [ ] Criar `lib/launch-studio-pro/agents/jl-partner-advisor.ts` (AC: 2, 3)
  - [ ] Input: nicho + avatar + launch_type
  - [ ] Guard: `if (launch_type !== 'joint') return null`
  - [ ] Output: `{ recommended: true, partner_profile, commission_structure }` conforme schema
- [ ] Persistir outputs (AC: 4, 5)
  - [ ] Copy nos campos de fases do `payload.sequence`
  - [ ] `payload.joint_launch` (null ou objeto conforme PRD §9.2)
- [ ] Criar API route (AC: 6)
  - [ ] POST `/api/launch-studio-pro/projects/[id]/generate/copy`
  - [ ] Executa `copywriter_agent` sempre, `jl_partner_advisor` condicionalmente
- [ ] Testes (AC: 7)
  - [ ] Lançamento não-conjunto: `jl_partner_advisor` não é chamado
  - [ ] Lançamento conjunto: `payload.joint_launch.recommended = true`
- [ ] Lint + typecheck (AC: 8)

## Dev Notes

### Pack de e-mails (PRD §2)
O copywriter gera:
- E-mails de pré-pré (aquecimento)
- E-mail de PLC 1 (Oportunidade)
- E-mail de PLC 2 (Transformação)
- E-mail de PLC 3 (Propriedade)
- E-mail de abertura do carrinho
- E-mails de follow-up (2-3)
- E-mails de fechamento (últimas 24h)

### Scripts de vídeo PLC (PRD §2)
Scripts palavra-por-palavra para PLC 1, 2 e 3. Incluídos em `sequence.plc1.script`, `plc2.script`, `plc3.script`.

### Schema joint_launch (PRD §9.2)
```json
{
  "recommended": true,
  "partner_profile": "Infoprodutor de público complementar com lista de 5k+",
  "commission_structure": "30% de comissão no carrinho"
}
```

### Dependência
Depende de 5.4 (sequência deve existir como input do copywriter).

### Testing

- Mock de copywriter retornando e-mails válidos: payload populado
- `launch_type = 'internal'`: `jl_partner_advisor` não executado, `joint_launch.recommended = false`
- `launch_type = 'joint'`: `jl_partner_advisor` executado, `joint_launch.recommended = true`

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-06-18 | 1.0 | Story criada | River (@sm) |

## Dev Agent Record

### Agent Model Used
_A preencher pelo @dev_

### Debug Log References
_A preencher pelo @dev_

### Completion Notes
_A preencher pelo @dev_

### File List
_A preencher pelo @dev_

## QA Results
_A preencher pelo @qa_
